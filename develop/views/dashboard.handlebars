{{#if blogs.length}}
   <h3>Current Projects:</h3>
   {{#each blogs as |blog|}}
       <div class="col-sm-5 mb-4 project lightBlue border border-primary rounded">
        <div class="col-md-5">
            <h2>
                <a href="/blog/{{blog.id}}">{{blog.name}}</a>
            </h2>
            <p>Created by {{name}} on {{format_date blog.date_created}}</p>
            <p>
                {{blog.description}}
            </p>
        </div>

    </div>
    {{/each}}
{{/if}}

{{!-- <div class="row gap-4 d-flex justify-content-center">
{{#each users as |user| }}
    <div class="col-sm-5 mb-4 project lightBlue border border-primary rounded">
        <div class="col-md-5">
            <h2>
                <a href="/blog/{{user.blogs.id}}">{{user.blogs.name}}</a>
            </h2>
            <p>Created by {{user.name}} on {{format_date user.blogs.date_created}}</p>
            <p>
                {{user.blogs.description}}
            </p>
        </div>

    </div>
{{/each}}
</div> --}}